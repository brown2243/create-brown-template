import type { Framework } from "src/utils/types";
import {
  blue,
  // cyan,
  // green,
  // lightBlue,
  // lightGreen,
  // lightRed,
  // magenta,
  // red,
  // reset,
  // yellow,
} from "kolorist";

export const DEFAULT_TARGET_DIR = "template-project";
export const FRAMEWORKS: Framework[] = [
  {
    name: "react",
    display: "React",
    color: blue,
  },
];
